import firebase from 'firebase';
const db = firebase.database();

export const flipThreeCards = (stackRef) => {
    //TODO: add functionality to remove the top 3 cards from StackBig and add those 3 cards to StackDrawn for same player in firebase, and trigger subsequent dispatch(es) to redux store.
}
